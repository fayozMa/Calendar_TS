import { FC } from "react";
import Calendar from "./components/Calendar";
const App: FC = () => {
  return (
    <div className="container mx-auto">
      <Calendar />
    </div>
  );
};

export default App;
